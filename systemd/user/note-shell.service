[Unit]
Description=Note Desktop shell
PartOf=note-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/note-shell
Restart=on-failure
RestartSec=1
Slice=session.slice

[Install]
WantedBy=note-session.target
