#!/bin/sh
if command -v gtklock >/dev/null 2>&1; then exec gtklock; fi
if command -v swaylock >/dev/null 2>&1; then exec swaylock -f -c 11141a; fi
loginctl lock-session "${XDG_SESSION_ID:-}" 2>/dev/null || true
